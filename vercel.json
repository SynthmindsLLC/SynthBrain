{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm --filter @synthbrain/web... build",
  "installCommand": "corepack enable && corepack prepare pnpm@10.4.1 --activate && pnpm -v && pnpm install --no-frozen-lockfile",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}

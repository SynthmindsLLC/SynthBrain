name: Vercel Preview

on:
  pull_request:
    paths:
      - 'apps/web/**'
      - 'packages/**'
      - 'pnpm-lock.yaml'
      - 'package.json'
      - '.github/workflows/preview-deploy.yml'

jobs:
  deploy-preview:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - uses: actions/setup-node@v4
        with:
          node-version: 20.18.0

      - name: Install
        run: pnpm install --no-frozen-lockfile

      - name: Deploy preview
        id: vercel
        if: ${{ env.HAS_VERCEL_TOKEN == 'true' }}
        env:
          HAS_VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN != '' }}
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          working-directory: ./apps/web

      - name: Note when Vercel secrets missing
        if: ${{ env.HAS_VERCEL_TOKEN != 'true' }}
        env:
          HAS_VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN != '' }}
        run: echo "VERCEL_TOKEN not set; preview deploy skipped. Link the repo in Vercel and add VERCEL_TOKEN/VERCEL_ORG_ID/VERCEL_PROJECT_ID secrets to enable previews via this workflow (Vercel's own Git integration also deploys automatically once linked)."

      - name: Comment preview URL
        if: success() && steps.vercel.outputs.preview-url != ''
        uses: thollander/actions-comment-pull-request@v3
        with:
          message: |
            🌐 SynthBrain preview deployed: ${{ steps.vercel.outputs.preview-url }}
